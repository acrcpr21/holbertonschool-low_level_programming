#include <stdio.h>

/**
* main - Entry main do while numbers
* Return: Always 0 (Success)
*/

int main(void)
{
    int number;
    number = 48;
    do {
        putchar(number);
        number++;

    } while (number <= 57);

    putchar ('\n');
    return (0)
}
